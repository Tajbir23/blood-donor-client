NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_JWT_TOKEN=tajbir
NEXT_PUBLIC_NODE_ENV=development

# Server-to-server auth key — Must match the SERVER_SECRET_KEY in the backend .env
SERVER_SECRET_KEY=blood-donor-server-secret-key-change-in-production
